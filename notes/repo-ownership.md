---
date: 2026-06-28
audience: humans + agents wiring a new repo into the smalltalk network
purpose: codify the convention for which agent owns which repo, and where that mapping lives today vs where it's headed
---

# Repo ownership

Most agents on this network are scoped to one repo. The convention is
informal but consistent enough to be load-bearing for the
manager/worker patterns in
[`agent-roles.md`](agent-roles.md) (the "one worker per repo" rule).
This doc names the convention and points to the upcoming formal
mechanism.

## The current convention

**Identity name encodes the repo binding.** Specifically:

- For Claude Code workers: `<repo-name>-claude`. Examples:
  `smalltalk-claude` owns the `smalltalk` repo, `pty-claude` owns
  `pty`, `pty-relay-claude` owns `pty-relay`.
- For Codex workers: `<repo-name>-codex`, or just `<repo-name>`
  when unambiguous (e.g. `vauban`).
- For multi-repo orchestrators or human-shaped identities: bare
  names that don't fit the pattern (`myobie`, `cos`, `pty-claude`'s
  manager incarnation).

This is convention-only — nothing in smalltalk enforces or even reads
it. An `agents` listing today doesn't carry "owns repo X" data
because there's nowhere to put it.

## Where the binding actually lives

At runtime, the binding lives in two per-machine files inside the
repo:

1. **`pty.toml`** — the source-of-truth runtime binding. The
   `[sessions.claude].env.ST_AGENT` (or `[sessions.codex].env`)
   value says "when this repo's session starts, this is the
   identity it runs as." Per-machine, gitignored. (`ST_IDENTITY`
   is the deprecated form and is still honored; the legacy
   `ST_IDENTITY` is not.)
2. **`.mcp.json`** — generated by `st init` in the
   repo. Wires the MCP server into the agent's host so the agent
   can call `st_msg_*` tools. Also per-machine.

Neither file is discoverable from smalltalk's data tree. To answer
"which agent owns repo X?" from outside the repo you currently:

- Look at the identity name (apply the convention above).
- OR open the repo and read its `pty.toml`.
- OR ask the agent directly: `st message send <agent> --subject
  "which repo are you in?"`.

## Why this matters

The convention is what lets a manager apply the **repo-boundary
rule** from [`agent-roles.md`](agent-roles.md):

> One worker per repo. The repo is the unit of context. A worker
> that's been working in `/path/to/smalltalk` has loaded files, knows
> the codebase shape, has commit history in head. Sending it work in
> `/path/to/some-other-repo` flushes that context.

Without an identity ↔ repo mapping the manager can't route work
correctly. The naming convention makes routing inference-free for
the common case ("work in `pty` → `pty-claude`").

## Edge cases the convention doesn't cover

- **Multi-repo agents.** A single identity that works across several
  related repos (e.g. a release-engineering identity touching `pty`
  + `smalltalk` + `pty-relay`). Today these are bare-named (`cos`,
  `myobie`) and rely on context to know which repo a given message
  is about.
- **Multiple agents per repo.** When the
  [`agent-roles.md`](agent-roles.md) parallelism rule fires (one
  worker on tests, one on src), there's no naming pattern that
  encodes the split. Today both identities are spun up ad-hoc with
  manual identity names (`smalltalk-tests-claude` vs
  `smalltalk-src-claude`; historically these took the form
  `st-tests-claude` / `st-src-claude`).
- **Manager vs worker for the same repo.** The agent named
  `smalltalk-claude` (formerly `st-claude` — the underlying
  identity was renamed during Phase 3 of brief-005) could be
  operating as a worker on a brief, or as a manager spawning a child
  worker, depending on its system prompt for that session. The
  identity name doesn't disambiguate. In practice the human knows
  the role from the pty session's tags (`role = "agent"` vs unset).

These are tolerable today because the network is small. They'd
become unworkable at scale.

## Where this is headed: resources

**brief-009 item 5** introduces a `<agent>/resources/` folder. Each
resource is a markdown file whose frontmatter carries a `url`. The
intent is exactly to formalize cases like this one — an agent
declares which artifacts it owns / relates to. A future-shape
example:

```
<root>/smalltalk-claude/resources/
  1782300000000-a1b2c3.md   # this agent's repo
  1782300100000-d4e5f6.md   # an open PR
  1782300200000-g7h8i9.md   # a pty session it supervises
```

```yaml
# 1782300000000-a1b2c3.md
---
url: https://github.com/compoundingtech/smalltalk
title: smalltalk repo (owns)
tags: [repo, owns]
---
The agent owns work in this repo. See pty.toml in the repo for the
runtime identity binding.
```

Once resources ship, the manager doesn't need to apply the naming
convention — it can `st resource ls <agent>` and read the declared
bindings directly. The convention stays as a sane default when nobody
has bothered to declare resources, but the data becomes the source
of truth.

A `pty://` URL scheme is also planned for the same release —
agents will link their pty session as a resource so peers can
identify them by pty-session identity directly, not just by
identity-name string.

## Until then

- New agent in a new repo? Name it `<repo>-claude` (or
  `-codex` / bare). Set `ST_AGENT` in `pty.toml`. That's the
  binding.
- New agent in an existing repo? Pick a name that doesn't collide
  with the existing one and is descriptive (`smalltalk-tests-claude`,
  `smalltalk-research-claude`). Re-evaluate the parallelism rule
  before spawning — most repos want one worker, not two.
- Cross-repo orchestrator identity? Use a bare name. Document the
  scope in the agent's system prompt or memory; don't try to encode
  it in the identity name.

## See also

- [`onboarding.md`](onboarding.md) — step-by-step
  recipe for wiring a new agent.
- [`agent-roles.md`](agent-roles.md) — manager/worker roles and the
  one-worker-per-repo heuristic.
- [`actor-model.md`](actor-model.md) — why identities map to
  agents, not to repos.
